{
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "lint": "turbo run lint",
    "tegami": "node scripts/tegami.mts",
    "test": "turbo run test"
  },
  "devDependencies": {
    "tegami": "^1.2.2",
    "turbo": "^2.10.3"
  }
}
