#!/bin/sh
export GARDN_AGENT=codex
printf '%b' '\033[2J\033[H\033]0;⠋ billing\007'
cat <<'EOF'
  lumen / billing

  > add the tax preview

  compiling invoice totals
  ⠋ writing tests
EOF
DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec python3 "$DIR/hold.py"
