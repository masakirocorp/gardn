#!/bin/sh
export GARDN_AGENT=omp
printf '%b' '\033[2J\033[H\033]0;omp · catalog\007'
cat <<'EOF'
  lumen / catalog

  index the new merch collection

  Allow tool: write
  Approve  Deny
EOF
DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec python3 "$DIR/hold.py"
