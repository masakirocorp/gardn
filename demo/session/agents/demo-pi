#!/bin/sh
export GARDN_AGENT=pi
printf '%b' '\033[2J\033[H\033]0;pi · checkout\007'
cat <<'EOF'
  checkout

  > split the cart form into two steps

  drafting the review pane
  billing is still writing the tax path

  Working...
EOF
DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec python3 "$DIR/hold.py"
