---
packages:
  omh: patch
---

### Launch the public website

The Oh My Herdr product site and manual now publish from Cloudflare at `ohmyherdr.com` with production canonical metadata, static search, immutable versioned Local API schemas, security headers, and explicit source-install and release gates. The launch review also removed an invalid tagged-source installation path before publication.
