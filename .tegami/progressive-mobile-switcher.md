---
packages:
  omh: patch
  omh-docs: patch
---

### Navigate mobile workspaces one level at a time

The compact mobile switcher now moves through groups, spaces, tabs, and panes one level at a time. A persistent breadcrumb, aligned counts, contextual creation actions, and a separate actions screen make the hierarchy easier to scan and navigate with either mouse or keyboard.
