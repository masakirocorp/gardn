---
packages:
  gardn: patch
---

### Show SSH test results on a live connection

Testing a saved SSH connection now reports success or failure immediately, including when the host is already connected.

### Save an SSH group default without typing a directory

Clicking the host field in a new group dialog cycles Local and saved SSH connections. Saving an SSH host with an empty directory keeps that host and uses the connection's suggested directory.
