---
packages:
  gardn: patch
---

# Make ghui the curated GitHub tool

Use ghui as the default GitHub project command for new configurations and command resets. Gardn launches ghui in the workspace repository with mouse controls, visible scrollbars, and an isolated terminal-theme configuration. Remove the obsolete Group GitHub organization setting.
