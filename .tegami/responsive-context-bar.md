---
packages:
  omh: patch
---

### Add a responsive workspace context bar

Desktop clients now show a clickable, client-local group / workspace / tab path with live topology counts. The bar hides duplicate context while the spaces sidebar is expanded by default, supports per-client toggling, and remains usable on narrow terminals.
