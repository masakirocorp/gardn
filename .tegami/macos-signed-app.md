---
packages:
  gardn: patch
---

### Ship a signed macOS extra

macOS releases now include a signed, notarized `Gardn.app` disk image. Drag it to Applications. PATH `gardn` stays the GitHub CLI release.
