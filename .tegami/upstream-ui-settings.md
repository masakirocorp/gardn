# Add terminal UI controls

You can now choose dot or symbol agent status indicators, set the outer terminal window title, configure the headless terminal size, and route right-clicks to individual panes. Disabling automatic copy now keeps mouse selections visible until you copy or clear them.
