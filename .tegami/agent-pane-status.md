---
packages:
  omh: patch
---

### Improve agent status in pane borders

Pane borders can now show an agent name with its live status, while Pi and OMP agents remain working when compaction completes and only become idle after the agent actually ends.
