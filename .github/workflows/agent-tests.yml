name: Agent Fixture Tests

on:
  pull_request:
    paths:
      - .github/workflows/agent-tests.yml
      - .github/workflows/live-agent-tests.yml
      - ci/agent-tests/**
      - apps/hako/src/manifests/maki.toml
      - apps/hako/src/integration/assets/**
      - apps/hako/src/integration/mod.rs
  push:
    branches: [master]
    paths:
      - .github/workflows/agent-tests.yml
      - .github/workflows/live-agent-tests.yml
      - ci/agent-tests/**
      - apps/hako/src/manifests/maki.toml
      - apps/hako/src/integration/assets/**
      - apps/hako/src/integration/mod.rs
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: agent-fixture-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    name: image and fixture checks
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Build image
        run: docker build -t hako-agent-tests:ci ci/agent-tests

      - name: Check installed CLIs
        run: docker run --rm hako-agent-tests:ci

      - name: Check provider env wiring
        run: >-
          docker run --rm
          -e OPENROUTER_API_KEY=sk-hako-agent-test
          hako-agent-tests:ci
          hako-agent-tests-env hako-agent-tests-verify-env

      - name: Check plugin fixtures
        run: |
          node --experimental-strip-types ci/agent-tests/pi-omp-plugin-status-test.mjs apps/hako/src/integration/assets/pi/hako-agent-state.ts pi
          node --experimental-strip-types ci/agent-tests/pi-omp-plugin-status-test.mjs apps/hako/src/integration/assets/omp/hako-agent-state.ts omp
          node ci/agent-tests/qoder-openrouter-proxy-test.mjs
